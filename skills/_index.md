# Skills Index

> Scan this to identify which skills apply to the current task, then read the full SKILL.md for patterns and code examples.

## Generic Skills

| Skill              | When to load                                                              |
| ------------------ | ------------------------------------------------------------------------- |
| `typescript`       | Any `.ts` or `.tsx` file — types, no `any`, no `enum`, `as const`        |
| `react-patterns`   | Creating or refactoring React components, custom hooks, composition       |
| `nextjs-core`      | Server Components, Server Actions, pages, layouts, `app/` directory       |
| `ui-engineering`   | Tailwind v4, shadcn, Aceternity, `cn()`, design tokens                   |
| `ux`               | Designing user flows, CRUD dashboards, validation UX, recovery states     |
| `database`         | Supabase queries, RLS policies, service layer with Supabase client        |
| `prisma`           | Prisma schema, queries, migrations, service layer with Prisma ORM         |
| `security`         | Auth checks, input validation, XSS/CSRF, server-side protection           |
| `error-handling`   | Custom error classes, Error Boundaries, structured logging                |
| `testing`          | Vitest, Testing Library, MSW, unit/integration/E2E tests                  |
| `api-design`       | Route Handlers, webhooks, external API integrations                       |
| `git-workflow`     | Commits, branching, PRs, Conventional Commits                             |
| `i18n`             | next-intl, locale routing, translation keys                               |
| `accessibility`    | WCAG 2.1, ARIA, keyboard navigation, screen readers                       |
| `performance`      | Core Web Vitals, lazy loading, React Compiler, bundle optimization        |
| `seo`              | Meta tags, Open Graph, structured data, sitemap generation                |
| `state-management` | Zustand, React Context, global/shared state, persistence                  |
| `remotion`         | Video creation in React, frame-based animations, rendering pipeline       |
| `react-native`     | Expo, React Navigation, native APIs, mobile-specific patterns             |
| `env-config`       | Zod env validation, server/public var separation, fail-fast at startup    |

## Meta Skills

| Skill           | Purpose                                                    |
| --------------- | ---------------------------------------------------------- |
| `skill-creator` | Create a new skill following the standard template         |
| `skill-sync`    | Sync skill metadata to AGENTS.md                           |
| `feedback-loop` | Capture improvement signals and learnings                  |
| `project-setup` | Document project context in CLAUDE.md                      |

---

_Skills Index v1.7 | Total: 24 skills_
